queue<int>q;
q.push(node)
while(q.empty()==false){
    int node = q.top()
    q.pop();
    visited[node]=1;
    for(int i->0 to adj[node].size()){
        if(!visited[]){
            q.push()
        }
    }
}